import { HeaderContainer } from "./style";
import { NavBar } from "./NavBar";
import { Headers } from "./Headers";

export const Header = () => {
  return (
    <HeaderContainer>
      <Headers />
      <NavBar />
    </HeaderContainer>
  );
};
